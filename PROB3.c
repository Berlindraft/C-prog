#include <stdio.h>

int main(){
  int num1 = 4;
  int num2 = 5;
  int product;

  product = num1 * num2;
  printf("The product of 4 and 5 is: %d", product);

    /*given that we already have values we multiply both
    to get a product */

    getch();
    return 0;
}
